{
  "name": "worker1",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild index.js --bundle --loader:.css=text --loader:.html=text --loader:.svg=text --outfile=dist/index.js"
  },
  "dependencies": {
    "esbuild": "^0.27.2"
  }
}
